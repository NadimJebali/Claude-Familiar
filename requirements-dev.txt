# Dev/test-only dependencies. The shipped widget stays pure standard library —
# nothing here is ever imported by mascot/ or hooks/ at runtime.
#
#   python -m pip install -r requirements-dev.txt
#
pytest
pytest-qt>=4        # Qt test helpers for the PySide6 view layer (ADR-0002)
hypothesis>=6
ruff>=0.15
mypy>=1.8
